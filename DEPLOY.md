# 🚀 Инструкция по деплою проекта на GitHub

## Шаг 1: Инициализация Git репозитория

```powershell
# Инициализировать git в текущей папке
git init

# Проверить статус (должны быть видны файлы проекта)
git status
```

## Шаг 2: Добавление файлов

```powershell
# Добавить все файлы (кроме тех, что в .gitignore)
git add .

# Или добавить файлы по отдельности:
git add .gitignore
git add requirements.txt
git add README.md
git add ffmpeg_editor.py
```

## Шаг 3: Первый коммит

```powershell
# Сделать первый коммит
git commit -m "Initial commit: FFmpeg Video Editor with Uniquification features"
```

## Шаг 4: Создание репозитория на GitHub

1. Перейдите на [GitHub.com](https://github.com)
2. Нажмите кнопку **"New repository"** (зелёная кнопка справа или "+" в правом верхнем углу)
3. Введите название репозитория (например: `ffmpeg-video-editor`)
4. Выберите **Public** или **Private**
5. **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license" (всё это уже есть)
6. Нажмите **"Create repository"**

## Шаг 5: Привязка удалённого репозитория

```powershell
# Замените YOUR_USERNAME и YOUR_REPO_NAME на ваши данные
# Пример: git remote add origin https://github.com/username/ffmpeg-video-editor.git
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

# Проверить, что репозиторий привязан
git remote -v
```

## Шаг 6: Пуш в ветку main

```powershell
# Переименовать ветку в main (если нужно)
git branch -M main

# Запушить код в GitHub
git push -u origin main
```

## 📝 Полная последовательность команд (скопируйте и выполните)

```powershell
# 1. Инициализация
git init

# 2. Добавление файлов
git add .

# 3. Первый коммит
git commit -m "Initial commit: FFmpeg Video Editor with Uniquification features"

# 4. Привязка удалённого репозитория (ЗАМЕНИТЕ URL!)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO_NAME.git

# 5. Переименование ветки
git branch -M main

# 6. Пуш в GitHub
git push -u origin main
```

## 🔧 Последующие обновления

После изменений в коде:

```powershell
# Добавить изменения
git add .

# Закоммитить
git commit -m "Описание изменений"

# Запушить
git push
```

## ⚠️ Важные замечания

1. **Не забудьте заменить** `YOUR_USERNAME/YOUR_REPO_NAME` на реальный URL вашего репозитория!
2. При первом push GitHub может запросить **логин и токен доступа** (Personal Access Token)
3. Если возникнут ошибки с аутентификацией, используйте GitHub CLI или настройте SSH ключи

## 🔐 Настройка аутентификации GitHub

Если возникнут проблемы с push:

### Вариант 1: Personal Access Token
1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Выберите scope: `repo`
4. Скопируйте токен и используйте его как пароль при push

### Вариант 2: GitHub CLI
```powershell
# Установить GitHub CLI (если не установлен)
winget install GitHub.cli

# Авторизоваться
gh auth login

# Затем обычный push
git push
```
