# 🎬 FFmpeg Video Editor + Уникализация

Приложение для редактирования и **уникализации** видео с использованием FFmpeg и предпросмотром изменений в реальном времени.

## ✨ Ключевые функции уникализации

### 🖼️ Canvas Effect (Холст)
- **Размытый фон** — оригинальное видео увеличенное и размытое
- **Передний план** — уменьшенное видео с закруглёнными углами
- **Центрирование** — точное наложение по центру
- **Виньетка** — поверх композиции

### 🎵 Audio Pitch
- Изменение высоты тона **без изменения скорости**
- Диапазон 0.95 - 1.05 (незаметно для уха)
- Уникализирует аудиодорожку

### 📋 Метаданные
- **Полная очистка** EXIF и метаданных
- **Случайные значения** title, handler_name, creation_time
- Обход детекции дубликатов

## 📋 Требования

- Python 3.8+
- FFmpeg установлен и доступен в PATH
- Windows 10/11

## 🚀 Установка

### 1. Установка FFmpeg

**Windows:**
1. Скачайте FFmpeg: https://github.com/BtbN/FFmpeg-Builds/releases
2. Распакуйте архив (например, в `C:\ffmpeg`)
3. Добавьте `C:\ffmpeg\bin` в переменную PATH

Или через winget:
```powershell
winget install ffmpeg
```

Или через chocolatey:
```powershell
choco install ffmpeg
```

### 2. Установка Python зависимостей

```bash
pip install -r requirements.txt
```

## ▶️ Запуск

```bash
python ffmpeg_editor.py
```

## 🎨 Возможности

### 🎨 Цветокоррекция
- **Яркость** (-1 до 1)
- **Контраст** (0 до 3)
- **Насыщенность** (0 до 3)
- **Гамма** (общая и по каналам RGB)
- **Оттенок (Hue)** (-180° до 180°)

### 🔧 Фильтры
- **Резкость** (unsharp)
- **Размытие** (boxblur)
- **Шумоподавление** (nlmeans)
- **Виньетка**

### 📐 Геометрия
- **Поворот** (0°, 90°, 180°, 270°)
- **Отражение** (горизонтальное/вертикальное)
- **Масштабирование** (с пресетами 480p, 720p, 1080p, 4K)
- **Обрезка** (crop)

### ⚡ Эффекты
- **Скорость воспроизведения** (0.25x - 4x)
- **Чёрно-белый режим**
- **Негатив**

### 🎭 Уникализация (НОВОЕ!)

#### Canvas Effect
| Параметр | Диапазон | Описание |
|----------|----------|----------|
| Масштаб видео | 0.7 - 0.95 | Размер переднего плана |
| Размытие фона | 5 - 50 | Интенсивность blur |
| Радиус углов | 0 - 50 | Закругление углов |
| Zoom фона | 1.0 - 1.3 | Увеличение фона |
| Виньетка | 0 - 1.0 | Затемнение краёв |

#### Audio Pitch
| Параметр | Диапазон | Описание |
|----------|----------|----------|
| Pitch | 0.95 - 1.05 | Изменение тона без изменения скорости |

#### Метаданные
- Очистка всех EXIF данных (`-map_metadata -1`)
- Генерация случайных: title, handler_name, encoder, creation_time

#### Быстрые пресеты уникализации
- 🟢 **Лёгкая** — только pitch + метаданные
- 🟡 **Средняя** — Canvas + pitch + метаданные
- 🔴 **Сильная** — максимальный Canvas + все эффекты

### 📋 Пресеты цвета
- Винтаж
- Холодный
- Тёплый
- Драматичный
- Приглушённый
- Яркий

### 📝 Свой фильтр
Возможность ввести произвольный фильтр FFmpeg (без `-vf`).

## 📖 Как пользоваться

1. **Загрузите видео** - нажмите кнопку "📂 Загрузить видео"
2. **Настройте параметры** - используйте вкладки справа для изменения настроек
3. **Смотрите превью** - изменения отображаются в реальном времени
4. **Перемещайтесь по видео** - используйте слайдер времени для выбора кадра
5. **Копируйте команду** - нажмите "📋 Копировать команду" для использования в терминале
6. **Экспортируйте** - нажмите "💾 Экспорт видео" для сохранения результата

## 🔧 Примеры FFmpeg команд

### Базовые операции
```bash
# Базовая цветокоррекция
ffmpeg -i input.mp4 -vf "eq=brightness=0.2:contrast=1.3:saturation=1.2" output.mp4

# Масштабирование до 720p
ffmpeg -i input.mp4 -vf "scale=1280:720" output.mp4

# Ускорение в 2 раза
ffmpeg -i input.mp4 -vf "setpts=0.5*PTS" -af "atempo=2" output.mp4

# Винтажный эффект
ffmpeg -i input.mp4 -vf "curves=vintage" output.mp4
```

### 🎭 Уникализация

```bash
# Canvas Effect (размытый фон + уменьшенное видео с закруглёнными углами)
ffmpeg -i input.mp4 -filter_complex "[0:v]split=2[bg][fg];[bg]scale=2208:1242,boxblur=25:25,crop=1920:1080[bg_out];[fg]scale=1632:918,format=rgba,geq='r=r(X,Y)':g='g(X,Y)':b='b(X,Y)':a='if(lt(X,20)*lt(Y,20),if(lte(hypot(20-X,20-Y),20),255,0),if(gt(X,W-20)*lt(Y,20),if(lte(hypot(X-W+20,20-Y),20),255,0),if(lt(X,20)*gt(Y,H-20),if(lte(hypot(20-X,Y-H+20),20),255,0),if(gt(X,W-20)*gt(Y,H-20),if(lte(hypot(X-W+20,Y-H+20),20),255,0),255))))'[fg_rounded];[bg_out][fg_rounded]overlay=(W-w)/2:(H-h)/2:format=auto[composed];[composed]vignette=PI/13.33[final]" -map "[final]" -map 0:a output.mp4

# Audio Pitch (изменение тона без изменения скорости)
ffmpeg -i input.mp4 -af "asetrate=45276,aresample=44100" output.mp4

# Очистка метаданных + случайные данные
ffmpeg -i input.mp4 -map_metadata -1 -metadata title=RandomTitle123 -metadata handler_name=VideoHandler_abc123 output.mp4

# Полная уникализация (Canvas + Pitch + Metadata)
ffmpeg -i input.mp4 -filter_complex "..." -af "asetrate=45100,aresample=44100" -map_metadata -1 -metadata title=UniqueVideo output.mp4
```

## ⌨️ Горячие клавиши

- **Пробел** - воспроизведение/пауза (когда курсор в окне превью)

## 🐛 Решение проблем

### FFmpeg не найден
Убедитесь, что FFmpeg установлен и добавлен в PATH:
```bash
ffmpeg -version
```

### Превью не обновляется
- Проверьте, что видео файл не повреждён
- Попробуйте другой формат видео
- Проверьте консоль на наличие ошибок

### Низкое качество превью
Превью генерируется как один кадр для быстрой работы. Финальный экспорт будет в полном качестве.

## 📝 Лицензия

MIT License

